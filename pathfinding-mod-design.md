# Minecraft 経路探索MOD 設計書

Xaero's Map連携・Google Map風の最短経路表示（NeoForge / MC 1.21.1 / シングルプレイ）

---

## 0. 検証状況について（重要）

本書の内容は、以下の実物ソースコードを取得・確認した上で書いています。

| 参照元 | 取得内容 | 信頼度 |
|---|---|---|
| **XaeroPlus** (rfresh2, MIT) | 2026-08-06時点の最新コミット。Xaero WorldMap **1.44.2** / Minimap **26.4.2**（現行最新）に対応 | ◎ 実コード確認済 |
| **Baritone** (cabaletta, LGPL) | コスト計算ロジック全般 | ◎ 実コード確認済 |
| Minecraft Wiki | ブロック硬度・ツール速度・エリトラ物理 | ◎ 公式wiki確認済 |

**注意点**：XaeroPlusは対象MCバージョンが1.20.1です。`xaero.*` パッケージ内のクラス構造は同じXaeroバージョン系列なのでほぼ流用できますが、MC標準クラスに触れる部分（`PoseStack` / `GuiGraphics` 周辺）は1.21.1で差異があります。**1.21.1向けのXaero devアーティファクトを取得して最終確認するステップは、実装前に必ず必要です。**

---

## 1. 前回の推測からの重要な訂正

前回「たぶんここにフックする」と挙げたクラスパスは、**現行版では移動していました**。

| 前回の推測 | 現行版の実際 |
|---|---|
| `xaero.common.minimap.element.render.over.MinimapElementOverMapRendererHandler` | `xaero.hud.minimap.element.render.over.MinimapElementOverMapRendererHandler`（`common` → `hud` に移動） |
| `xaero.map.gui.RightClickOption` | `xaero.map.gui.dropdown.rightclick.RightClickOption` |
| `xaero.map.gui.ScreenBase` | `xaero.lib.client.gui.ScreenBase`（共通ライブラリ xaerolib へ分離） |

さらに、参考にしていた `plusls/xaero-map-addition` は1.16〜1.18世代のコードで、**現行版ではそのままビルドが通りません**。参照先をXaeroPlusに全面的に切り替えるべきです。

---

## 2. Xaero連携：確定した設計

### 2-1. フック先（XaeroPlusで実証済みの経路）

XaeroPlusは「マップ上に独自の線を描く」問題を既に解決済みで、Drawing Tool機能として線分・楕円・ハイライトを両マップに描画しています。**これはやりたいこととほぼ同一**なので、この構造をそのまま踏襲するのが最適解です。

**世界地図（フルスクリーン）側**
- 対象：`xaero.map.gui.GuiMap#render`
- 手法：`@WrapOperation` で `MultiBufferSource$BufferSource#endBatch()`（ordinal 0）の呼び出しを包み、その直後に自前描画を挿入
- 座標取得：`@Local` でメソッド内のローカル変数 `flooredCameraX` / `flooredCameraZ` / `matrixStack` / `renderTypeBuffers` / `fboScale` を直接キャプチャ

**ミニマップ側**
- 対象：`MinimapFBORenderer#renderChunksToFBO`
- 手法：`@WrapOperation` で `xaero.common.mods.SupportXaeroWorldmap#drawMinimap(...)` の呼び出しを包む
- 洞窟モード時は別経路（`MultiTextureRenderTypeRendererProvider#draw`）にも同じフックが必要 ← **見落としやすい重要ポイント**
- 座標変換：`xFloored`/`zFloored` からビットシフトでチャンク・タイル・内部座標に分解

### 2-2. 座標変換の実装（XaeroPlus実装より）

```
// 世界地図
matrixStack.pushPose();
matrixStack.translate(-flooredCameraX, -flooredCameraZ, 1.0f);
  → 以降はワールドのブロック座標をそのまま指定して描画できる
matrixStack.popPose();

// ミニマップ
cameraBlockX = chunkX * 64 + tileX * 16 + insideX;
cameraBlockZ = chunkZ * 64 + tileZ * 16 + insideZ;
matrixStack.translate(-cameraBlockX, -cameraBlockZ, 0);
```

この「カメラ位置を打ち消す平行移動を1回かけて、あとは絶対ブロック座標で描く」パターンにすることで、ミニマップと世界地図で描画コードを共通化できます。

### 2-3. 必須の依存ライブラリ

- **MixinExtras** — `@WrapOperation` / `@Local` / `@ModifyExpressionValue` は標準Mixinには存在しません。XaeroPlusはこれに全面依存しており、上記のフック手法はMixinExtras前提です。NeoForge 1.21.1にはバンドル済みなので追加設定は不要です。
- **xaerolib** — Xaero系MODの共通ライブラリ。`implementation "xaero.lib:xaerolib-forge-<mcver>:+"`

### 2-4. Maven設定

```gradle
repositories {
    maven { url "https://chocolateminecraft.com/maven"; name "Xaero's Maven" }
}
dependencies {
    implementation "xaero.lib:xaerolib-forge-1.21.1:+"
    implementation "xaero.map:xaeroworldmap-forge-1.21.1:<version>"
    implementation "xaero.minimap:xaerominimap-forge-1.21.1:<version>"
}
```

1.21.1以降はMojang公式マッピングでビルドされているため、`:dev` 分類子は不要（1.21.1未満とは指定方法が異なる点に注意）。

### 2-5. 壊れにくくするための方針

Xaeroは更新履歴を見る限り、レンダリング周りを繰り返し大規模リファクタしています（実際に上記の通りパッケージ移動が発生済み）。したがって:

1. **経路データと描画ロジックはXaeroに一切依存させない**。純粋な「ポリライン + 色」のデータ構造として保持
2. Xaero依存部分は「フックしてカメラ座標とmatrixStackを受け取る」薄いアダプタ層のみに閉じる（≒XaeroPlusの`DrawManager`相当）
3. `IMixinConfigPlugin` で対象クラスの存在を事前チェックし、無ければMixinを適用しない。Xaero未導入でもクラッシュせず、ワールド内描画だけで動作する構成にする
4. **ワールド内（3D空間）への経路描画は`RenderLevelStageEvent`で完全に自前実装**しておく。これならXaeroが壊れてもMODの中核機能は生き残る

---

## 3. 掘削コストモデル（確定）

### 3-1. 基本式

```
掘削tick数 = 硬度 × 30 / ツール速度      （適正ツール）
           = 硬度 × 100 / ツール速度     （不適正ツール・素手）
+ 固定オーバーヘッド 2 tick（振り向き・ツール切替分）
```

- ツール速度：木2 / 石4 / 鉄6 / ダイヤ8 / ネザライト9 / **金12**
- Efficiency Lv があれば速度に `+(Lv² + 1)`（適正ツール時のみ有効）
- 比較基準：徒歩1マス ≈ **4.63 tick** / スプリント1マス ≈ **3.56 tick**

### 3-2. 主要ブロック早見表（鉄ツール = 速度6）

| ブロック | 硬度 | tick | 徒歩比 | 判断 |
|---|---|---|---|---|
| ネザーラック | 0.4 | 4.0 | 0.9x | 掘る方が速い |
| 土・砂 | 0.5 | 4.5 | 1.0x | ほぼ互角 |
| 砂利 | 0.6 | 5.0 | 1.1x | ほぼ互角 |
| 石 | 1.5 | 9.5 | 2.1x | 2マス以内の迂回なら迂回 |
| 丸石 | 2.0 | 12.0 | 2.6x | 迂回優先 |
| 深層岩 | 3.0 | 17.0 | 3.7x | 強く迂回優先 |
| 深層岩の丸石 | 3.5 | 19.5 | 4.2x | 強く迂回優先 |
| 黒曜石（ダイヤ） | 50 | 189.5 | 41x | 実質禁止扱い |

### 3-3. 特殊ケースの扱い

| ケース | 扱い |
|---|---|
| 掘る対象の真上が砂/砂利 | そのブロックの掘削コストも再帰加算（落下対策） |
| 水 | 通行可、コストは徒歩の約2倍（水中1マス ≈ 9.09 tick） |
| 溶岩 | 進入不可（コスト無限大） |
| 掘削禁止ブロック | チェスト・かまど・スポナー・原木・作業台などをデフォルト禁止。設定で追加可能に |
| 硬度が負のブロック | 岩盤・バリア等。破壊不可（コスト無限大） |
| ツール選定 | ホットバー内の最適ツールを自動選択して計算 |

### 3-4. コスト計算と安全確認の分離

コスト計算はあくまで**事前見積もり**です。経路として提示する直前に、別レイヤーで以下を再チェックすること:

- 掘った先が溶岩・奈落に直結していないか
- 頭上に落下ブロックが積み上がっていないか
- 掘削後に水が流入する構造でないか

---

## 4. A*アルゴリズム本体（徒歩・掘削モード）

### 4-1. ノードとエッジ

座標 `(x, y, z)` をノードとし、移動を**タイプ別のエッジ**として分離します。Baritoneの分類を踏襲するのが安全です。

| 移動タイプ | 概要 |
|---|---|
| Traverse | 水平前進（掘削を含む場合あり） |
| Diagonal | 斜め移動（両隣が通行可能な場合のみ） |
| Ascend | 1マス上る |
| Descend | 1マス下る |
| Fall | 落下（落下ダメージをコストに加算） |
| Pillar | ブロックを置いて登る（※初期実装では省略可） |

### 4-2. ヒューリスティック

A*の最適性を保つには**過小評価**が必須です。

```
h(n) = 残り直線距離 × 3.56（スプリント1マスのコスト）
```

これは「どんなに好条件でもこれより安くはならない」という下限であり、許容的ヒューリスティックとして機能します。掘削コストを含む経路では実コストがこれを大きく上回るため、安全側に働きます。

### 4-3. 探索範囲の制限

- 開始地点と目的地を含むバウンディングボックス + マージン（水平±64マス程度）に限定
- Y方向は `-64 〜 320` の範囲だが、実際には現在位置と目的地のYを含む帯 + マージンに絞る
- 未探索チャンクは直線で仮結線し、楽観的コスト（スプリント相当）で見積もる → 探索された瞬間に再計算

### 4-4. 打ち切り条件

以下のいずれかで打ち切り、その時点で**ゴールに最も近づけた暫定経路**を返す:

- 展開ノード数の上限（目安：10,000〜50,000）
- 計算時間の上限（目安：数百ms）

「経路が見つからない」より「不完全でも何か表示する」方がGoogle Map風UXとして正しい挙動です。

### 4-5. スレッド設計

**メインスレッドでワールドを直接触ってはいけません**（並行変更で例外・処理落ちの原因）。

```
[メインスレッド]
  ↓ 探索範囲のブロック状態をスナップショット化（軽量キャッシュ）
[ワーカースレッド]
  ↓ A*実行
  ↓ 結果のポリラインを返す
[メインスレッド]
  ↓ 描画データを差し替え
```

Baritoneの`BlockStateInterface`が同じ思想の実装例です。

### 4-6. 再計算トリガー

| トリガー | 条件 |
|---|---|
| 逸脱 | プレイヤーが経路から2〜3マス以上離れた |
| 地形更新 | 新規ロードされたチャンクが経路と重なった |
| ゴール変更 | ピンが変更・削除された |
| 定期実行 | 保険として1〜2秒間隔 |

計算中に次のトリガーが来た場合は、**古い計算をキャンセルして最新のものだけ走らせる**（結果の追い越しを防ぐ）。

---

## 5. エリトラ飛行モード（別アルゴリズム）

### 5-1. 前提となる実際の物理値（公式wiki確認済）

- 最大効率時のグライド比：**約10:1**（水平10マスにつき高度1マス低下）
- 最低速度：約7.2 m/s（30°の上向きピッチ時）
- 速度が上がるほど空気抵抗も増える連続的な物理。急旋回は大きく減速する

離散的なマス移動ではないため、徒歩用グリッドA*の流用は不適切です。

### 5-2. 3段構えのアルゴリズム

**Step 1：直線判定**
出発点→目的地の直線上を5〜10マス間隔でレイキャストし、地形との交差だけをチェック。交差なしならこれで完了（大半のケースがここで終わる）。

**Step 2：高度上げて再判定**
交差ありなら、その区間の最高地形高度 + マージン（10〜20マス）まで上昇する経路に置き換えて再判定。ほとんどの山はこれで越えられます。

**Step 3：粗いグリッドA*にフォールバック**
それでもダメな区間（谷・洞窟内など）のみ、**4〜8マス刻みの粗いグリッド**でA*。徒歩用の1マス刻みは不要なので、計算量は徒歩A*よりはるかに軽く済みます。

極端なケース（すり鉢状の地形など）は「一旦上昇してから直進」で近似し、精度は妥協して構いません。

### 5-3. モード切替

MVP段階では**ユーザーが明示的に選択**する方式。エリトラ展開の自動検知は後回しで問題ありません。

---

## 6. 実装ロードマップ

### Phase 0：検証（最優先・実装前に必ず）
1. 1.21.1向けXaero devアーティファクトをMavenから取得
2. Vineflower等でデコンパイルし、本書 §2-1 のクラス・メソッドが現行1.21.1版にも存在するか照合
3. **ダミー描画（単色の四角）だけをInject**し、想定通りの座標・タイミングで呼ばれるか確認
4. 洞窟モード時のミニマップ描画経路も忘れずに確認

→ ここが通らなければ以降の設計を見直す。**最大のリスクポイント**

### Phase 1：コア
5. ブロック状態スナップショット層
6. コスト関数（§3の表を実装）
7. A*本体（Traverse/Ascend/Descend のみ。斜め・落下は後回し）
8. 非同期実行とキャンセル機構

### Phase 2：描画
9. ワールド内描画（`RenderLevelStageEvent`）← Xaero非依存なので先に作る
10. Xaeroアダプタ層（世界地図）
11. Xaeroアダプタ層（ミニマップ + 洞窟モード）

### Phase 3：仕上げ
12. 再計算トリガー
13. 掘削の残りエッジケース（落下ブロック連鎖・液体）
14. エリトラモード
15. 設定画面（掘削許可のON/OFF、禁止ブロックリスト等）

---

## 7. 参考にすべきコードベース

| プロジェクト | ライセンス | 参考にする部分 |
|---|---|---|
| **XaeroPlus** | MIT | Xaeroへのフック手法全般、DrawManager構造、線描画実装。**最重要参照先** |
| **Baritone** | LGPL v3 | コスト計算、移動タイプ分類、BlockStateInterface。※LGPLなのでコード流用時はライセンス要注意。アイデア参考に留めるのが安全 |

---

## 8. 未確定・要判断の項目

- [ ] Xaeroの1.21.1版クラス構造の最終確認（Phase 0）
- [ ] Baritoneのコードを直接流用するか、アイデア参考に留めるか（LGPLの扱い）
- [ ] 経路の視覚表現（線の色・太さ・アニメーション・高低差の表現方法）
- [ ] 掘削経路を「別色で表示」するか（Google Map風に「有料道路」的な区別があると親切）
- [ ] 対応MCバージョンの拡大戦略（Xaeroは多数のバージョンに対応しているが、レンダリング周りの差異が大きい）
